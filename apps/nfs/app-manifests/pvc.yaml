apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-share
  namespace: nfs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 225Gi

